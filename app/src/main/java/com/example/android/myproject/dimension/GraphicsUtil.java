package com.example.android.myproject.dimension;

import com.example.android.myproject.R;
import com.jjoe64.graphview.GraphView;
import com.jjoe64.graphview.series.BarGraphSeries;
import com.jjoe64.graphview.series.DataPoint;
import com.jjoe64.graphview.series.LineGraphSeries;

public class GraphicsUtil {

    // Graph View
    GraphView graphView;


    /**
     * Пoстроение графика
     */
    /*
    // Находим graphView по id
    graphView = binding.idGraphView;

    // Добавляем точки в наш graphView
    BarGraphSeries<DataPoint> series = new BarGraphSeries<>(new DataPoint[] {
            new DataPoint(0,6.1),
            new DataPoint(1,3.9),
            new DataPoint(2,7.0),
            new DataPoint(3,8.1),
            new DataPoint(4,5.9),
            new DataPoint(5,6.8),
            new DataPoint(6,4.8),
            new DataPoint(7,4.1)
    });

    // Title
        graphView.setTitle("Graph View");
    // Color
        graphView.setTitleColor(R.color.purple_200);
    // Добавить точки к graphView
        graphView.addSeries(series);

    // red Line
    LineGraphSeries<DataPoint> lineUp = new LineGraphSeries<>(new DataPoint[]{
            new DataPoint(0, 3.3),
            new DataPoint(8, 3.3)
    });
        graphView.addSeries(lineUp);

    LineGraphSeries<DataPoint> lineDown = new LineGraphSeries<>(new DataPoint[]{
            new DataPoint(0, 6.6),
            new DataPoint(8, 6.6)
    });
        graphView.addSeries(lineDown);

*/

}
